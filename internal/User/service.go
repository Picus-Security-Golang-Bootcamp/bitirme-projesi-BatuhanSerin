package User

// func Login(email, password string) (User, error) {
// 	var user User
// 	if err := *&gorm.DB{}.Where("email = ? AND password = ?", email, password).First(&user).Error; err != nil {
// 		return nil, err
// 	}
// 	return user, nil
// }
